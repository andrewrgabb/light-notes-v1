type Note @model @key(fields: ["id"]) {
  id: String!
  name: String
  content: String
}

type Column @model @key(fields: ["id"]) {
  id: String!
  name: String
  noteOrder: String
}

type ColumnOrder @model @key(fields: ["id"]) {
  id: String!
  ids: String
}
