type Note @model @key(fields: ["id"]) {
  id: String!
  name: String!
  content: String!
  sessionId: String!
}

type Board @model @key(fields: ["id"]) {
  id: String!
  json: String!
  sessionId: String!
}
