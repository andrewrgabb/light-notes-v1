type Note @model @key(fields: ["id"]) {
  id: String!
  title: String!
  content: String!
  sessionId: String!
}

type Board @model @key(fields: ["id"]) {
  id: String!
  json: String!
  sessionId: String!
}
