type Note @model @auth(rules: [{allow: owner}]) @key(fields: ["id"]) {
  id: String!
  title: String!
  content: String!
  sessionId: String!
}

type Board @model @auth(rules: [{allow: owner}]) @key(fields: ["id"]) {
  id: String!
  json: String!
  sessionId: String!
}
